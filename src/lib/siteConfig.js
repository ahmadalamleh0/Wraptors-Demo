// Single source of truth for the production domain, so every canonical/OG
// URL and the sitemap always agree — never hardcode this string elsewhere.
export const SITE_URL = 'https://wraptorsdubai.com';
